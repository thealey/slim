<%= simple_form_for @bp do |f| %>
  <%= f.association :person, :as => :hidden %>
  <%= f.input :dia, :label => 'Diastolic' %>
  <%= f.input :sys, :label => 'Systolic' %>
  <%= f.button :submit, :class => 'btn-primary' %>
<% end %>
