<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Slim" %></title>
    <%= stylesheet_link_tag "blueprint/screen.css" %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <link href='http://fonts.googleapis.com/css?family=Snippet|Pompiere|Loved+by+the+King|Muli&v2' rel='stylesheet' type='text/css'>
    <%= yield(:head) %>
    <script type="text/javascript">

      var _gaq = _gaq || [];
      _gaq.push(['_setAccount', 'UA-24669302-1']);
      _gaq.push(['_trackPageview']);

      (function() {
       var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
       ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
       var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
       })();

  </script>

  </head>
  <body>
    <div id="container">

      <div class="nav">
        <table>
          <tr>
            <td>
              <% if logged_in? %>
                Logged in as  <%= link_to current_person.username, :controller=>"people", :action=>"edit" %>.
                <%= link_to "Log out", logout_path %>
              <% else %>
                <%= link_to "Sign up", signup_path %> or
                <%= link_to "log in", login_path %>.
              <% end %>
            </td>
            <% if logged_in? %>
              <td>
                <%= link_to "Home", :controller=>"measures", :action=>"updateall" %>
              </td>
              <td>
                <%= link_to "Update Withings", :controller=>"measures", :action=>"updateall"   %>
              </td>
              <td>
                <%= link_to "New Measure", new_measure_path %>
              </td>
              <td>
                <%= link_to "Import All from Withings",  :controller=>"measures", :action=>"importall" %>
              </td>
              <!--
              <td>
                <%= link_to "Update Trend",  :controller=>"measures", :action=>"update_trend" %>
              </td>
              -->
            <% end %>
          </tr>
        </table>
      </div>
      <center>
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
    </center>
    </div>
    @2011 Ted Healey<br/>
    Powered by the <a href="http://www.withings.com/">Withings Body Scale</a><br/>
    Codes at <a href="https://github.com/thealey/slim">https://github.com/thealey/slim</a>
  </body>
</html>
